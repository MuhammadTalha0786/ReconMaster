# Future Improvements

- Replace regex-based report parsing with full Nmap XML parsing throughout
- Add scan-result diffing to highlight changes between runs over time
- Optional `python-nmap` backend for richer structured data
- Scheduled/recurring scans with historical trend charts
- SIEM-friendly export formats (CEF, Syslog)
- Packaged distribution (pipx / standalone executables via PyInstaller)
